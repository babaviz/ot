{% extends '::base.html.twig' %}

{% block body -%}
    <h1>BookedTime list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Start_time</th>
                <th>End_time</th>
                <th>Status</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('teacher_bookedtime_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.starttime }}</td>
                <td>{{ entity.endtime }}</td>
                <td>{{ entity.status }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('teacher_bookedtime_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('teacher_bookedtime_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('teacher_bookedtime_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
